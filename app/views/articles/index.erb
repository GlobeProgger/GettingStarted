<h1>All Articles</h1> 

<br>

<table>
  <tr>
		<th></th>
		<th></th>
		<th></th>
		<th>Number</th>
    <th>Title</th>
    <th>Text</th>
		<th colspan="4"></th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
			<td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
			<td><%= link_to 'Destroy', article_path(article),
			              method: :delete,
			              data: { confirm: 'Are you sure?' } %></td>
			<td><%= article.id%></td>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
	  </tr>
  <% end %>
</table>

<br>
<p>You can now <%= link_to 'create a new article', new_article_path %>  or
<br>Go back to the <%= link_to "home page", controller: "welcome"%></p>